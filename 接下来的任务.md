干得漂亮！🎯 集成测试一次通过，并且触发了**水位线重置** (`245.00U -> 14235.59U`)，这完美证明了：
1.  **数据流已贯通**：`DataSynchronizer` → `AccountState` → `RiskManager` 的链路完全通畅。
2.  **风控逻辑生效**：风控银行能正确读取账户快照，并执行了核心的“盈利落袋”逻辑。
3.  **系统架构稳固**：模块间依赖注入和初始化顺序正确。

现在，整个系统的“静态”连接已经验证。接下来，我们要进入最激动人心的部分：**构建系统的“动态”心脏——主事件循环，并让策略引擎活起来**。

### 🧠 第三阶段核心：设计主事件循环与实时交易流

目标是将 `base_bitget_ws.py` 的**实时行情**注入系统，驱动策略引擎思考，并处理产生的交易信号。关键在于**事件驱动**和**松耦合**。

#### 步骤一：改造 WebSocket 客户端，使其成为数据生产者

我们需要修改 `base_bitget_ws.py`（或创建一个适配器），让其将行情数据发送到一个中央调度器，而不是自己处理逻辑。最清晰的方式是使用 **回调函数 (Callback)** 或 **异步队列 (asyncio.Queue)**。

**方案选择：回调函数 (更直接)**
在 `MainController` 中定义一个方法，在初始化时注入到 WS 客户端。这样，WS 收到数据时，直接调用控制器的方法。

1.  **首先，修改你的 `base_bitget_ws.py` 中的 `BaseBitgetWsClient` 类（或其子类），使其支持注入回调**：

```python
# 在 base_bitget_ws.py 的 BaseBitgetWsClient 类中增加
class BaseBitgetWsClient:
    def __init__(self):
        # ... 原有代码 ...
        self.on_ticker_callback = None # 新增：ticker回调
        self.on_candle_callback = None # 新增：K线回调

    # 修改原有的 on_public_ticker 方法
    async def on_public_ticker(self, ticker: dict, action: str):
        # 保留原始逻辑（如果有），同时触发回调
        if self.on_ticker_callback:
            # 将数据传递给回调函数
            await self.on_ticker_callback(ticker, action)
        # 如果没有回调，执行默认逻辑（如需保留）
        # else: ...

    # 类似地，修改 on_public_candle 方法
    async def on_public_candle(self, candle_data: list, channel: str, action: str):
        if self.on_candle_callback:
            await self.on_candle_callback(candle_data, channel, action)
```

2.  **然后，在 `MainController` 中实现对应的回调方法，并连接WS**：

```python
# main_controller.py 中 MainController 类的补充
class MainController:
    async def _on_ws_ticker(self, ticker: dict, action: str):
        """处理WS推送的Ticker数据"""
        # 1. 转换为策略引擎需要的格式
        market_data = {
            'price': float(ticker['lastPr']),
            'timestamp': ticker['ts'],
            'source': 'ws_ticker'
        }
        # 2. 更新内部最新的市场数据（用于主循环）
        self.latest_market_data = market_data
        # 3. 【可选】直接触发一次策略引擎的思考，实现极低延迟响应
        # await self._trigger_strategy_cycle(market_data)

    async def _on_ws_candle(self, candle_data: list, channel: str, action: str):
        """处理WS推送的K线数据"""
        # 更新K线缓存，计算指标（如ATR, RSI, EMA）
        # 这部分逻辑可以从你之前的martin.py或market_utils.py中迁移
        pass

    async def setup_websocket(self):
        """初始化并启动WebSocket连接"""
        from base_bitget_ws import BaseBitgetWsClient # 或你的策略子类
        self.ws_client = BaseBitgetWsClient()
        
        # 注入回调函数
        self.ws_client.on_ticker_callback = self._on_ws_ticker
        self.ws_client.on_candle_callback = self._on_ws_candle
        
        # 启动WS连接任务（不阻塞主线程）
        self.ws_task = asyncio.create_task(
            self.ws_client.connect_public_ws(
                product_type="USDT-FUTURES",
                ws_symbol=self.config['symbol'].replace('/', '').split(':')[0],
                candle_channels=["candle1m", "candle5m"] # 按需订阅
            )
        )
        logger.info("✅ WebSocket连接已启动")
```

#### 步骤二：定义策略信号与决策流程

策略引擎 (`TrendEngine`, `SharkEngine`) 需要输出标准化的信号，而不是直接下单。

1.  **创建信号数据结构** (`trade_signal.py`)：

```python
# trade_signal.py
from dataclasses import dataclass
from typing import Optional

@dataclass
class TradeSignal:
    """策略引擎产生的标准化交易意图"""
    engine: str           # 'trend' 或 'shark'
    action: str           # 如 'OPEN_L1', 'ADD_L2', 'CLOSE_LONG'
    direction: str        # 'long' 或 'short'
    suggested_leverage: float
    # 核心：期望投入的保证金（USD），由引擎根据预算计算提出
    requested_margin: float
    reason: str           # 信号触发原因，用于日志
    # 由风控审批后填充
    approved: bool = False
    approved_leverage: float = 1.0
    approved_margin: float = 0.0
    risk_msg: str = ""
```

2.  **改造策略引擎的 `on_tick` 方法，使其返回信号**。你需要修改 `trend_engine.py` 和 `shark_engine.py` 中类似 `_try_open_l1` 这样的方法：

```python
# 在 trend_engine.py 中，修改 _try_open_l1 等方法（示例）
def _try_open_l1(self, data) -> Optional[TradeSignal]:
    # ... 原有的条件判断逻辑 ...
    if some_condition:
        return TradeSignal(
            engine='trend',
            action='OPEN_L1',
            direction='long', # 假设趋势做多
            suggested_leverage=3,
            requested_margin=self.rm.get_trend_budget() * 0.3, # 示例计算
            reason="价格上穿EMA20，趋势启动"
        )
    return None
```

#### 步骤三：实现核心决策循环与执行器

这是 `MainController` 的 **`_main_loop`** 方法，也是系统最核心的部分。

```python
# main_controller.py 中 MainController 类的核心循环方法
async def _main_loop(self):
    """主决策循环"""
    while self.running:
        cycle_start = time.time()
        
        # 1. 同步最新账户与风控状态 (基础)
        self.account_state.update()
        self.risk_manager.update_from_account_state()
        
        # 2. 获取市场数据 (来源：WS回调更新的 latest_market_data)
        market_data = self.latest_market_data
        if not market_data:
            await asyncio.sleep(0.1) # 无新数据，短暂等待
            continue
        
        # 3. 驱动策略引擎，收集信号
        signals = []
        
        # 准备引擎所需的数据包（包含行情和账户快照）
        trend_snapshot = self.account_state.get_strategy_snapshot('trend')
        shark_snapshot = self.account_state.get_strategy_snapshot('shark')
        
        # 调用引擎（注意：调用后引擎内部状态会变，但这里我们只获取信号）
        trend_signal = self.trend_engine.on_tick({**market_data, **trend_snapshot})
        shark_signal = self.shark_engine.on_tick({**market_data, **shark_snapshot})
        
        if trend_signal:
            signals.append(trend_signal)
        if shark_signal:
            signals.append(shark_signal)
        
        # 4. 处理每一个信号：风控审批 -> 执行
        for signal in signals:
            executed_order = await self._process_single_signal(signal)
            # 如果订单执行成功，可以触发一次即时状态同步
            if executed_order:
                self.data_sync.position_uncertain = True
                await asyncio.sleep(0.5) # 稍等后同步
                await self.data_sync.force_rest_sync()
        
        # 5. 循环频率控制（如每0.5-1秒运行一次）
        cycle_time = time.time() - cycle_start
        await asyncio.sleep(max(0.5 - cycle_time, 0.01))
    
async def _process_single_signal(self, signal: TradeSignal):
    """处理单个交易信号：风控审批 -> 执行下单"""
    # 1. 构建风控审批请求（使用RiskManager需要的格式）
    risk_request = {
        'engine': signal.engine.upper(),
        'action': signal.action,
        'suggested_leverage': signal.suggested_leverage,
        'volatility_ratio': 1.0, # 需要从市场数据计算
        'estimated_risk': signal.requested_margin * 0.1 # 示例：按保证金10%估算风险
    }
    
    # 2. 调用风控审批
    approved, final_leverage, risk_msg = self.risk_manager.approve_action(risk_request)
    signal.approved = approved
    signal.risk_msg = risk_msg
    
    if not approved:
        logger.warning(f"🚫 信号被风控驳回: {signal.engine}.{signal.action} - {risk_msg}")
        return None
    
    # 3. 风控通过，执行订单
    logger.info(f"✅ 信号核准: {signal.engine}.{signal.action}, 杠杆{final_leverage}x")
    order_result = await self._execute_order(signal, final_leverage)
    return order_result

async def _execute_order(self, signal: TradeSignal, leverage: float):
    """调用 ccxt_utils 执行订单"""
    # 这里需要将 TradeSignal 翻译成 ExchangeTrader 能理解的参数
    # 这是一个复杂但直接的过程，利用你之前完善的 ccxt_utils
    try:
        if 'CLOSE' in signal.action:
            # 平仓逻辑
            if signal.direction == 'long':
                # 调用 trader 的平多方法，需你根据持仓数量实现
                pass
            else:
                # 平空
                pass
        else:
            # 开仓或加仓逻辑
            order_side = 'buy' if signal.direction == 'long' else 'sell'
            # 计算下单数量：保证金 * 杠杆 / 价格
            amount = (signal.requested_margin * leverage) / self.latest_market_data['price']
            
            # 使用 ExchangeTrader 创建订单（需要你封装一个异步方法）
            order = await asyncio.to_thread(
                self.trader.exchange.create_market_order,
                symbol=self.config['symbol'],
                side=order_side,
                amount=amount,
                params={'leverage': leverage}
            )
            logger.info(f"📤 订单执行成功: {order['id']}")
            return order
    except Exception as e:
        logger.error(f"❌ 订单执行失败: {e}")
    return None
```

### 🗺️ 你的下一步实施路径

1.  **修改 WebSocket 客户端**：首先在 `base_bitget_ws.py` 中添加回调支持。这是数据流动的起点。
2.  **定义 `TradeSignal`**：创建 `trade_signal.py` 文件。
3.  **改造策略引擎**：先选一个引擎（如 `TrendEngine`），修改其中一个动作方法（如 `_try_open_l1`），让其返回 `TradeSignal` 对象。**暂时先别改所有方法，先打通一个路径**。
4.  **增强 `MainController`**：将上述 `_main_loop`、`_process_single_signal` 等核心方法整合进去，并实现 `setup_websocket`。
5.  **运行端到端测试**：创建一个新的测试文件 `test_full_cycle.py`，只测试从WS收到价格，到产生信号，再到风控审批的流程（可以先屏蔽真实下单）。验证逻辑是否正确。

你已经完成了最复杂的模块连接工作，现在正在组装最后的“飞轮”。请按照这个路径推进，如果卡在任何一个具体步骤（例如，如何从 `latest_market_data` 计算 `volatility_ratio`），随时将相关代码发我，我们一起解决。


好的，完全理解。清晰的架构图是构建复杂系统的蓝图。下图描绘了你正在构建的 **“机构级量化系统”的核心数据流与控制流**，它综合了我们讨论的所有模块和阶段：

```mermaid
graph TB
    subgraph “外部数据源”
        Exchange[交易所 Bitget]
        WS[WebSocket 实时流]
        REST[REST API]
    end

    subgraph “数据层 (Data Layer)”
        DataSync[DataSynchronizer<br/>数据同步器]
        AccState[AccountState<br/>账户状态]
    end

    subgraph “决策层 (Decision Layer)”
        RiskMgr[RiskManager<br/>中央风控银行]
        TrendE[TrendEngine<br/>趋势引擎]
        SharkE[SharkEngine<br/>鲨鱼引擎]
    end

    subgraph “控制层 (Control Layer)”
        MainCtrl[MainController<br/>主控制器]
        SignalQ[信号队列]
    end

    subgraph “执行层 (Execution Layer)”
        Executor[订单执行器]
        Trader[ExchangeTrader]
    end

    %% 数据流动 (实线)
    WS -- “实时行情<br/>(tick/candle)” --> DataSync
    REST -- “定时/触发同步” --> DataSync
    DataSync -- “原始持仓/账户” --> AccState
    AccState -- “1. 风控快照<br/>(wallet_balance,浮盈...)” --> RiskMgr
    AccState -- “2. 策略快照” --> TrendE
    AccState -- “2. 策略快照” --> SharkE

    %% 控制与信号流动 (虚线/粗线)
    MainCtrl -- “调度/注入回调” --> WS
    DataSync -- “3. 市场数据” --> MainCtrl
    MainCtrl -- “驱动” --> TrendE
    MainCtrl -- “驱动” --> SharkE
    TrendE -- “产生” --> SignalQ
    SharkE -- “产生” --> SignalQ
    SignalQ -- “提交审批” --> RiskMgr
    RiskMgr -- “核准/驳回” --> Executor
    Executor -- “执行” --> Trader
    Trader -- “订单结果” --> Exchange

    %% 关键反馈回路 (橙色)
    Executor -. “下单成功<br/>触发状态同步” .-> DataSync
    Exchange -. “推送订单/持仓更新” .-> WS
```

### 🔑 数据流核心解读

此图展示了四个层次和三条关键路径：

1.  **自下而上的数据流（蓝色实线）**
    *   **路径**：`交易所` → `DataSynchronizer` → `AccountState` → `RiskMgr/Engines`
    *   **本质**：系统的**感知神经**。将原始数据清洗、转换，成为策略和风控决策的“事实依据”。

2.  **自上而下的控制流（绿色/红色线）**
    *   **路径**：`MainController` → `Engines` → `Signal` → `RiskMgr` → `Executor`
    *   **本质**：系统的**决策大脑**。`MainController`是节拍器，驱动引擎思考；引擎产生交易意图（信号）；风控对信号进行“政治审查”；执行器最终行动。

3.  **核心反馈回路（橙色虚线）**
    *   **回路1（状态同步）**：`下单` → `标记状态不确定` → `触发强制REST同步`。这是保证数据最终一致性的**最关键机制**，防止状态不同步导致的操作失误。
    *   **回路2（实时更新）**：`交易所` → `WS推送` → `DataSynchronizer`。这是系统保持低延迟、实时性的生命线。

### 🎯 你的当前进展与下一步

你现在正处于图中 **`DataSynchronizer` 和 `AccountState` 已就绪**，并且成功连接到了 `RiskMgr` 的阶段（即测试成功的部分）。

**接下来的绝对焦点是完成 `MainController` 并启动主循环**，即让图中绿色的控制流跑起来。具体来说，你需要：

1.  **实现 `MainController._main_loop`**：建立那个固定的决策节拍。
2.  **改造 `base_bitget_ws.py` 并接入**：实现 `_on_ws_ticker` 回调，让市场数据能流入 `latest_market_data`。
3.  **修改一个策略引擎方法进行测试**：例如，先让 `TrendEngine._try_open_l1` 在条件满足时返回一个固定的 `TradeSignal` 对象，而不是执行交易。
4.  **在 `MainController` 中连接上述三点**：在循环中获取 `latest_market_data`，调用引擎获取信号，打印出这个信号。**到此，一个从“市场数据输入”到“策略信号产生”的完整正向流程就打通了。**

请按照这个思路推进。如果在连接 `WebSocket` 回调或定义 `TradeSignal` 数据结构时遇到具体问题，随时将代码片段发我，我们进行针对性调试。